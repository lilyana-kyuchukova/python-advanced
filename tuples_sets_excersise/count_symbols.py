text = tuple(item for item in input())
occ = dict()



# for letter in text:
#     if letter not in occ:
#         occ[letter] = 0
#     occ[letter] += 1
#
# sorted_dict = dict(sorted(occ.items()))
#
# [print(f"{key}: {value} time/s") for key, value in sorted_dict.items()]

